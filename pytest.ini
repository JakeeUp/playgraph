[pytest]
testpaths = tests

# pytest-asyncio warns on every run when this is unset, and the default is
# changing in a future release. Pinning it explicitly means the suite behaves
# the same before and after that change instead of shifting under us.
asyncio_default_fixture_loop_scope = function
